meta {
  name: create_topic
  type: http
  seq: 1
}

post {
  url: {{base_url}}/topics
  body: json
  auth: none
}

script:pre-request {
  req.setBody({
    topic: 'linked-list'
  })
}

tests {
  test("check status code", function() {
    expect(res.getStatus()).to.equal(201); 
  });
}
