meta {
  name: Health check
  type: http
  seq: 1
}

get {
  url: http://localhost:8000/
  body: none
  auth: none
}

tests {
  test("check status code", function() {
    expect(res.getStatus()).to.equal(200); 
  });
}
